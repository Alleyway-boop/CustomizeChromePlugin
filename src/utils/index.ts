// Re-export types from types.ts for backwards compatibility
export type { Message, Response, SendResponse, TabStatus, FreezeTabStatus } from '../types';
export type { PageInfoResponse, RestoreAllResult, OperationSuccess, OperationFailure, DebugStatusResponse, ResponseData, ExtendedTabStatus, BulkOperationResult, WhitelistExportData, WhitelistImportResult } from '../types';
